import React from "react"
import Layout from "components/Layout"

const ThankYouPage = () => (
  <Layout>
    <h1>You are subscribed!</h1>
    <p>New templates will now land in your inbox, stay tuned.</p>
  </Layout>
)

export default ThankYouPage
